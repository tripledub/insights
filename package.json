{
  "name": "insights",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "build": "echo 'Asset build process running...'",
    "build:watch": "echo 'Watching for asset changes...' && sleep infinity"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "*.rb": "bin/rubocop --force-exclusion"
  }
}
